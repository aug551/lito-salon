{
  "name": "litosalon",
  "version": "0.0.1",
  "description": "A static webpage for Lito Salon website",
  "main": "server.js",
  "scripts": {
    "start": "node ./server.js",
    "build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aaron Cruz",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3"
  },
  "engines": {
    "node": "16.14.0"
  }
}
